
    <p id="notification"><%= notice %></p>

    <p>
      <strong>Token:</strong>
      <%= @mailbox.token %>
    </p>

    <p>
      <strong>Name:</strong>
      <%= @mailbox.name %>
    </p>

    <p>
      <strong>Note:</strong>
      <%= @mailbox.note %>
    </p>

    <p>

      <strong>Form body:</strong>
      <%= form_code(@mailbox.token) %>
    </p>
